export default function taskBlock(trueOrFalse) { // eslint-disable-line no-unused-vars
  const task = false;
  const task2 = true;

  if (trueOrFalse) { // eslint-disable-line no-empty
    // eslint-disable-line no-undef
  }

  return [task, task2];
}
