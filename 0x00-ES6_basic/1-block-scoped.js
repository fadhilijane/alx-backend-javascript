export default function taskBlock(trueOrFalse) { // eslint-disable-line no-unused-vars
  const task = false;
  const task2 = true;

  // eslint-disable-next-line no-empty
  if (true0rFalse) { // eslint-disable-line no-undef

  }

  return [task, task2];
}
